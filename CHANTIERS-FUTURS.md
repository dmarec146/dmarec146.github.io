# Chantiers futurs

Liste des chantiers que David entreprendra une fois le travail de fond sur
les fiches (refonte, corrections) terminé. Ce n'est pas une liste de tâches
en cours — c'est un backlog pour plus tard.

**À mettre à jour uniquement à la demande de David** — ne pas y ajouter
d'éléments de sa propre initiative, contrairement à `SUIVI-FIREBASE.md`.

Fichier suivi par Git, synchronisé entre les machines via GitHub (`git push`
/ `git pull`), pas via Google Drive — même principe que `SUIVI-FIREBASE.md`.

## 1. Moteur robuste pour les plages de l'aléatoire

Travailler un moteur robuste pour mieux gérer les plages de l'aléatoire dans
toutes les fiches et les sujets blancs.

## 2. Banque d'automatismes

Mieux gérer les automatismes en début de fiche. Se créer une banque à partir
de critères définis.
